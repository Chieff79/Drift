// This page is no longer used. Speed test is now embedded in the home page.
// Kept as a stub to avoid breaking any remaining references.

import 'package:flutter/material.dart';

class SpeedTestPage extends StatelessWidget {
  const SpeedTestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Speed test has moved to the home page.')),
    );
  }
}
